class Utils {
  static String formatDate(DateTime date) {
    return '${date.day}/${date.month}/${date.year}';
  }

  static bool isNetworkAvailable() {
    // Placeholder for network check logic
    return true;
  }
}
